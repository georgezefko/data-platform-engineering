apiVersion: v1
kind: Service
metadata:
  name: kafka-connect-svc
  namespace: develop
  labels:
    app: kafka-connect
spec:
  type: ClusterIP
  ports:
    - name: "8083"
      port: 8083
      protocol: TCP
      targetPort: 8083
  selector:
    app: kafka-connect
